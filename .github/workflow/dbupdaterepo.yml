on:
 push
 
jobs:
 update:
  runs-on: ubuntu-latest
  
  steps:
    - name: Setup Python
      uses: actions/setup-python@v3
      with:
        python-version: 3.8
        
    - name: Install Daabricks CLI
      uses: databricks/setup-cli@main
      
    - name: Remote update Prod Repo
      env:
        Databricks_Host: ${{ secrets.Databricks_Host }}
        Databricks_Token: ${{ secrets.Databricks_Token }}
      run:
        databricks repos update "/Workspace/Users/sri.polati@gmail.com/GitHub Folders/Prod/test-repo-1" --branch main
